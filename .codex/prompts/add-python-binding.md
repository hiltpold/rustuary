# Add Python binding

Use this prompt for Codex:

Add or update the Python binding for the requested engine capability.

Constraints:

- Read root `AGENTS.md` and `bindings/python/AGENTS.md` first.
- Keep the Python API friendly for actuaries.
- Hide low-level `_rust` details.
- Add a minimal example in `bindings/python/examples/`.
- Do not add heavy dependencies without rationale.
- Summarize API usage and checks run.
