# Update API contract

Use this prompt for Codex:

Update the API contract for the requested workflow change.

Constraints:

- Read `contracts/README.md` and relevant service `AGENTS.md`.
- Keep large tabular payloads out of JSON; use object-store references or Arrow/Parquet artifacts.
- Add examples.
- Note any compatibility implications.
