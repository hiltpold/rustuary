# CLAUDE.md

This repository uses `AGENTS.md` as the source of truth for coding-agent instructions.

Read, in order:

1. Root `AGENTS.md`
2. The nearest nested `AGENTS.md` for the folder you are editing
3. Any relevant `.agents/skills/*/SKILL.md` workflow

Do not duplicate technology-specific rules here. Put durable rules in `AGENTS.md` or scoped skills so Codex, Claude, and other agents share the same guidance.
