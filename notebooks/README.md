# Notebooks

Notebook examples are for actuary-facing exploration and review. They should use the public Python API only.

Rules:

- Keep notebooks small and deterministic.
- Use files under `data/examples` or `data/golden`.
- Do not duplicate actuarial calculations in notebook cells.
- When a notebook demonstrates a public API, add or update a Python test for the same path.
